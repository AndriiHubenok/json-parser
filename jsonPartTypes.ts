type whitespace = " " | "\t" | "\n" | "\r";

type punct = "{" | "}" | "[" | "]" | "," | ":";

type trueType = "true";

type falseType = "false";

type nullType = "null";

type eof = "\n";

/*type value  = object | array | string | number | trueType | falseType | nullType;

type member = string ":" value;

type objectType = "{" [ member *( "," member ) ] "}"

type array  = "[" [ value *( "," value ) ] "]"*/